pipeline {


    environment {
        USER_CREDENTIALS = credentials('xxx')
    }

    stages {
        stage('pull') {
            steps{
                buildName "hello jenkins"
                container('test') {
                    git branch: "master", credentialsId: 'xxx', url: 'xxx'
                }
            }
        }
    }